package broadway.kyle.html;

import java.io.PrintStream;
import java.util.HashSet;
import java.util.Set;

public interface TagGeneric implements Renderer
{
    static final NullText NULL_TEXT = new NullText();

    Set<Renderer> toRenderSet = new HashSet<Renderer>();
    Text text = NULL_TEXT;
    String startTag = "<>";
    String endTag = "</>";

    public void render(PrintStream stream)
    {
        //Print the start tag
        stream.print(startTag);
        //Render all children
        for (Renderer renderer : toRenderSet)
        {
            renderer.render(stream);
        }
        //Print the end tag
        stream.print(endTag);
    }

    /**
     * Each tag must specify its own tags.  This method serves as
     * a reminder to do so.
     */
    public abstract void initTags();

}
